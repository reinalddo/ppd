# Material de trabajo en crudo (fotos sin editar, listas de precios, logo fuente).
# No debe quedar accesible mientras el demo está publicado.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
